{
  "name": "hilfreich",
  "version": "0.1.0",
  "description": "",
  "source": "src/index.ts",
  "main": "dist/index.js",
  "module": "dist/index.module.js",
  "umd:main": "dist/index.umd.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run build:lib && npm run build:widget",
    "build:widget": "microbundle build",
    "build:lib": "microbundle build -i src/Widget.tsx",
    "serve": "cross-env NODE_OPTIONS=--openssl-legacy-provider preact watch",
    "lint": "eslint '{src,tests}/**/*.{ts,tsx}'",
    "test": "jest",
    "tsc": "tsc"
  },
  "files": [
    "dist"
  ],
  "eslintConfig": {
    "parser": "@typescript-eslint/parser",
    "extends": [
      "preact",
      "plugin:@typescript-eslint/recommended"
    ],
    "ignorePatterns": [
      "build/"
    ]
  },
  "author": "Cristian Oliveira",
  "license": "MIT",
  "dependencies": {
    "preact": "10.5.7",
    "unfetch": "^5.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/preact": "^3.2.3",
    "cross-env": "^7.0.3",
    "jest-preset-preact": "^4.0.2",
    "microbundle": "^0.14.1",
    "preact-cli": "^3.0.5",
    "preact-habitat": "^3.3.0",
    "preact-render-to-string": "^5.1.11"
  },
  "jest": {
    "preset": "jest-preset-preact",
    "setupFilesAfterEnv": [
      "<rootDir>/jest-setup.js"
    ]
  }
}
